    ﻿Description:
      Folder synchronization task
    
    Usage:
      veeam_test_task [options]
    
    Options:
      -s, --source-directory <s> (REQUIRED)   Source directory.
      -r, --replica-directory <r> (REQUIRED)  Replica directory
      -i, --sync-interval <i>                 Synchronization interval (seconds) [default: 30]
      -l, --log-directory <l> (REQUIRED)      Log destination
      -?, -h, --help                          Show help and usage information
      --version                               Show version

To stop the synchronization press CTRL+C
